'use babel'
// @flow

import React from 'react';

export default class CuttingEdgeToolingPresentation extends React.Component<DefaultProps, Props, State> {

  state: State;
  props: Props;
  static defaultProps: DefaultProps;

  constructor(props: Props) {
    super(props);
  }

  render() {
    return (
      <div>

      </div>
    )
  }
}

CuttingEdgeToolingPresentation.propTypes = {

};

CuttingEdgeToolingPresentation.defaultProps = {

};

type DefaultProps = {

};

type Props = {

};

type State = {

};
